[project]
name = "recall"
version = "0.1.1"
description = "Session recall and analytics for AI agent sessions"
requires-python = ">=3.12"
dependencies = [
  "duckdb>=0.10.0",
  "pydantic>=2.6",
  "typer>=0.12",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.4",
  "ruff>=0.3",
]

[project.scripts]
recall = "recall.cli.app:app"
